name: shiire-kanri マニュアル GitHub Pages 配信

on:
  push:
    branches: [main]
    paths:
      - 'shiire-kanri/docs/manual/dist/**'
      - '.github/workflows/manual-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: ルート index.html を生成
        run: |
          mkdir -p _site
          cp shiire-kanri/docs/manual/dist/*.html _site/
          cp shiire-kanri/docs/manual/dist/*.pdf _site/
          cat > _site/index.html <<'EOF'
          <!doctype html>
          <html lang="ja"><head><meta charset="utf-8">
          <meta name="viewport" content="width=device-width,initial-scale=1">
          <title>shiire-kanri マニュアル</title>
          <style>
            body { font-family: -apple-system, "Hiragino Sans", sans-serif; max-width: 600px; margin: 40px auto; padding: 0 20px; color: #222; }
            h1 { color: #003d7a; }
            ul { list-style: none; padding: 0; }
            li { margin: 12px 0; padding: 16px; background: #f4f5f7; border-radius: 8px; }
            li a { color: #0066cc; text-decoration: none; font-weight: 600; font-size: 18px; }
            li small { display: block; color: #666; margin-top: 4px; }
          </style></head><body>
          <h1>📦 shiire-kanri マニュアル</h1>
          <ul>
            <li><a href="外注向けマニュアル.html">外注向けマニュアル（HTML）</a><small>スマホ・PCで閲覧、画像タップで拡大</small></li>
            <li><a href="外注向けマニュアル.pdf">外注向けマニュアル（PDF）</a><small>印刷・オフライン用</small></li>
            <li><a href="管理者向けマニュアル.html">管理者向けマニュアル（HTML）</a></li>
            <li><a href="管理者向けマニュアル.pdf">管理者向けマニュアル（PDF）</a></li>
          </ul>
          </body></html>
          EOF

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - id: deployment
        uses: actions/deploy-pages@v4
